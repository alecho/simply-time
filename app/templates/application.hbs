<nav class="navbar navbar-inverse navbar-fixed-top">
  {{#link-to "application" class="navbar-brand"}}Simply Time{{/link-to}}
  <ul class="nav nav-pills">
    <li class="nav-item">
      {{#link-to "clients" class="nav-link"}}Clients{{/link-to}}
    </li>
  </ul>
  {{#if session.isAuthenticated}}
    Logged in as {{session.currentUser.displayName}}
    <button {{action "signOut"}}>Sign out</button>
    {{outlet}}
  {{else}}
    <button {{action "signIn" "github"}}>Sign in with GitHub</button>
  {{/if}}
</nav>

<div class="container">
  {{outlet}}
</div>
